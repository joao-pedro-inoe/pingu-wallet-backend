# Porta do servidor backend
PORT=3000

# Configuração do Banco de Dados
# Se estiver rodando via docker-compose, estes devem alinhar com os serviços definidos
DB_HOST=localhost
DB_PORT=5432
DB_USER=pingu_user
DB_PASSWORD=pingu_password
DB_NAME=pingu_wallet_db

# Chave secreta para tokens JWT
JWT_SECRET=chave_secreta_pingu_wallet_2026